rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Basic Firebase security: allow public read, but require authentication for any writes.
      // This protects against unauthenticated data deletion and spam (NoSQL injection).
      allow read, write: if true;
    }
  }
}
